-- SQLite
-- CREATE TABLE todos (
--     id INTEGER PRIMARY KEY AUTOINCREMENT,
--     title TEXT NOT NULL,
--     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
--     completed BOOLEAN NOT NULL DEFAULT 0,
--     completed_at DATETIME NULL
-- );

-- insert into todos (title, completed) values ('todo1', 0);
-- insert into todos (title, completed) values ('todo2', 0);
-- insert into todos (title, completed) values ('todo3', 0);

select * from todos;